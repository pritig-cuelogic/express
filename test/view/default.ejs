<!DOCTYPE html>
<html lang="en">
  <head>
    <% include partials/pages/head.ejs  %>
  </head>
  <body class="<%= classname %>" >
  <% include partials/pages/header.ejs  %>
    <div class="container">
        <% if(typeof(device) != 'undefined') { %>
        	<p>Device data</p>
        	<ul>
        	<% for(i=0; i<device.length; i++) { %>
        		<li><%= device[i].device_id %></li>
        	<% } %>
        	</ul>
        <% } %>
        <% if(title == 'Login') { %>
            <p> Login </p>
            <% include partials/pages/login.ejs  %>
        <% } %>
       
	
	</div>
	<% include partials/pages/footer.ejs  %>
   <% include partials/pages/jsdefault.ejs  %>
  </body>
</html>

